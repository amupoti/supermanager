package org.amupoti.supermanager.acb.domain.model;

import lombok.Getter;

@Getter
public enum MarketCategory {
    NAME, TEAM, PRICE, PRICE_FORMATTED, BUY_PCT, LAST_VAL, MEAN_VAL, LAST_THREE_VAL, LAST_FOUR_VAL,
    PLUS_15_BROKER, KEEP_BROKER, FISIC_STATUS, ID_PLAYER, POSITION, IS_SPANISH, IS_FOREIGN;
}
